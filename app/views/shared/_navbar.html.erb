<nav class="navbar navbar-default" role="navigation">
<div class="container-fluid">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
      <%= image_tag 'network.png', height: '50px;' %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <% if current_user %>
      <ul class="nav navbar-nav">
        <li class="<%= active_link?('consumptions') %>">
          <%= link_to t('consumptions'), customer_consumptions_path(current_customer) %>
        </li>
        <li class="<%= active_link?('consumption_charts') %>">
          <%= link_to t('charts'), customer_consumption_charts_path(current_customer) %>
        </li>
      </ul>
    <% end %>
    <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">User<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to 'Home', root_path %></li>
            <li><%= link_to 'logout', destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
    <% end %>
  </div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>
